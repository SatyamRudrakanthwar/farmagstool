import os
from dotenv import load_dotenv
import streamlit as st

load_dotenv()  # Loads variables from .env

HF_TOKEN = os.getenv("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")  # Safe: token not in repo

# Use it in your classifier
classifier = load_clip_classifier(hf_token=HF_TOKEN)
